document.addEventListener("DOMContentLoaded",async()=>{const $=id=>document.getElementById(id);const activities=await bhActivities();const params=new URLSearchParams(location.search);if(params.get("region"))$("area").value=params.get("region");if(params.get("town"))$("town").value=params.get("town");if(params.get("age"))$("age").value=params.get("age");if(params.get("day"))$("day").value=params.get("day");if(params.get("free"))$("free").checked=true;const categoryMap={"Baby classes":"Baby","Baby & toddler":"Toddler","Family activities":"Family"};function render(){const q=$("search").value.trim().toLowerCase(),c=$("category").value,a=$("area").value,g=$("age").value,d=$("day").value,t=$("town").value,f=$("free").checked;const list=activities.filter(x=>(!q||x.title.toLowerCase().includes(q))&&(!c||categoryMap[x.category]===c)&&(!a||x.region===a)&&(!t||x.town===t)&&(!g||x.age_range.includes(g))&&(!d||x.day===d)&&(!f||/^(free|£0|0)$/i.test(String(x.price||"").trim())));$("count").textContent=list.length+" activit"+(list.length===1?"y":"ies")+" found";$("results").innerHTML=list.map(x=>'<article class="activity-card"><div class="activity-image">Activity image</div><div class="activity-body"><div class="activity-meta">'+(categoryMap[x.category]||x.category)+" · "+x.age_range.join(", ")+'</div><h3>'+x.title+'</h3><p>'+x.region+" · "+x.day+" · "+x.price+'</p><div class="activity-footer"><a class="text-link" href="activity.html?id='+encodeURIComponent(x.id)+'">View activity →</a><button class="button button-soft bh-save" data-id="'+x.id+'">'+(bhIsSaved(x.id)?"♥ Saved":"♡ Save")+'</button></div></div></article>').join("")||'<div class="admin-panel"><h3>No activities found</h3><p>Try changing your filters.</p></div>';document.querySelectorAll(".bh-save").forEach(b=>b.onclick=()=>{bhToggleSaved(b.dataset.id);render()})}["search","category","area","town","age","day","free"].forEach(id=>$(id).addEventListener("input",render));$("clear").onclick=()=>{["search","category","area","town","age","day"].forEach(id=>$(id).value="");$("free").checked=false;render()};render()}).catch(e=>{document.getElementById("results").innerHTML='<div class="admin-panel"><h3>Activities unavailable</h3><p>'+e.message+'</p></div>'});